name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Uncomment and customize one of these setup steps per project,
      # or add your own.

      # - name: Set up Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: "22"

      # - name: Set up Python
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: "3.12"

      # - name: Set up .NET
      #   uses: actions/setup-dotnet@v4
      #   with:
      #     dotnet-version: "9.0.x"

      - name: Install dependencies
        run: |
          echo "TODO: Replace this with your install command"
          # Examples:
          # npm install
          # pip install -r requirements.txt
          # dotnet restore

      - name: Run tests
        run: |
          echo "TODO: Replace this with your test command"
          # Examples:
          # npm test
          # pytest
          # dotnet test
